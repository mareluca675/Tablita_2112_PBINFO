#include <bits/stdc++.h> 

using namespace std;

int main(){
    ifstream cin("tablita.in");
    ofstream cout("tablita.out");
    
    long n, s, i;
    
    cin >> n; 
    
    for(i = 1; s + i < n; i++){
        s += i;
    } 
    
    cout << i;
}
